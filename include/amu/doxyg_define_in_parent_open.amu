/+

  define a group and add to a parent group and leave groups open.
  groups must be explicitly close via @} @}.

  requires:
                ${group*}               (from pgid_*.)
                ${parent}

 +/

  \addtogroup \amu_eval(${parent})
  @{

  \defgroup \amu_eval(${group} ${group_name})
  \brief    \amu_eval(${group_brief})
  @{

/+ end of file  +/
